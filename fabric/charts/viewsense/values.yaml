global:
  environment: development
  image:
    repository: viewsense-core
    tag: dev
    pullPolicy: Never
  identityUrl: https://identity:8443
  identityIssuer: https://identity:8443
  identityPublicSecret: identity-public
  workloadCredentialsSecret: workload-credentials
  podSecurity:
    runAsUser: 10001

products:
  identity:
    product: development
    issuer: ""
    jwksUrl: ""
    audience: viewsense
    tenantClaim: tenant_id
    scopeClaim: scope
    # JWKS/issuer network access for external identity. Empty is fail-closed.
    externalEgress: []
  llm:
    product: mock-openai-compatible
    endpoint: https://mock-llm:8443
    audience: mock-llm
    # Required for non-bundled endpoints when NetworkPolicy is enabled.
    # Standard NetworkPolicy accepts CIDRs, not DNS names.
    externalEgress: []
    # - cidr: 203.0.113.10/32
    #   port: 443
    openai:
      baseUrl: https://api.openai.com/v1
      model: gpt-5.6-luna
      credentialsSecret: openai-credentials
      # Use explicit provider CIDRs or an enterprise CNI FQDN/egress-proxy policy.
      externalEgress: []
  memory:
    product: postgres-pgvector
    endpoint: https://memory-postgres:8443
    audience: memory-postgres
    externalEgress: []
    mem0:
      mode: oss
      endpoint: https://mem0.example.invalid
      credentialsSecret: mem0-credentials
      externalEgress: []
      inCluster:
        enabled: false
        namespace: memory-platform
        podLabels:
          app.kubernetes.io/name: mem0
        port: 8443
  ingestion:
    product: builtin-deterministic
  agentRuntime:
    enabled: true
    product: builtin
  workflow:
    enabled: false
    product: none
  mcp:
    mockProviderEnabled: true
    allowedHosts: mock-mcp
  policy:
    enabled: false
    product: builtin
    endpoint: http://127.0.0.1:8181/v1/data/viewsense/provider/admit
    opa:
      image: openpolicyagent/opa:1.8.0-static
  workloadIdentity:
    product: static-development-pki
    spiffe:
      trustDomain: viewsense.local
      socketPath: unix:///spiffe-workload-api/spire-agent.sock
  observability:
    product: json-stdout
    otlpEndpoint: ""

modules:
  identity:
    enabled: true
    appModule: viewsense_identity.app
    plane: security
    tlsSecret: tls-identity
    ingressFrom: [gateway, orchestrator, ingestion, llm-gateway, memory-gateway, mcp-gateway, smoke]
    egressTo: []
    resources: {requests: {cpu: 50m, memory: 96Mi}, limits: {cpu: 500m, memory: 256Mi}}
  gateway:
    enabled: true
    appModule: viewsense_gateway.app
    plane: edge
    tlsSecret: tls-gateway
    clientId: gateway
    clientSecretKey: gateway
    ingressFrom: [smoke]
    egressTo: [identity, orchestrator]
    resources: {requests: {cpu: 50m, memory: 96Mi}, limits: {cpu: 500m, memory: 256Mi}}
  orchestrator:
    enabled: true
    appModule: viewsense_orchestrator.app
    plane: control
    tlsSecret: tls-orchestrator
    clientId: orchestrator
    clientSecretKey: orchestrator
    ingressFrom: [gateway]
    egressTo: [identity, memory-gateway, llm-gateway, mcp-gateway]
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}
  governance:
    enabled: true
    appModule: viewsense_governance.app
    plane: sovereign-control
    tlsSecret: tls-governance
    ingressFrom: [smoke]
    egressTo: [governance-db]
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}
  ingestion:
    enabled: true
    appModule: viewsense_ingestion.app
    plane: ingestion
    tlsSecret: tls-ingestion
    clientId: ingestion
    clientSecretKey: ingestion
    ingressFrom: [smoke]
    egressTo: [identity, memory-gateway]
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}
  llm-gateway:
    enabled: true
    appModule: viewsense_llm_gateway.app
    plane: llm-control
    tlsSecret: tls-llm-gateway
    clientId: llm-gateway
    clientSecretKey: llm-gateway
    ingressFrom: [orchestrator]
    egressTo: [identity, mock-llm, openai-adapter]
    resources: {requests: {cpu: 50m, memory: 96Mi}, limits: {cpu: 500m, memory: 256Mi}}
  mock-llm:
    enabled: true
    appModule: viewsense_mock_llm.app
    plane: llm-provider
    tlsSecret: tls-mock-llm
    ingressFrom: [llm-gateway]
    egressTo: []
    resources: {requests: {cpu: 25m, memory: 64Mi}, limits: {cpu: 250m, memory: 192Mi}}
  openai-adapter:
    enabled: true
    appModule: viewsense_openai_adapter.app
    plane: llm-provider
    tlsSecret: tls-openai-adapter
    ingressFrom: [llm-gateway]
    egressTo: []
    resources: {requests: {cpu: 50m, memory: 96Mi}, limits: {cpu: 500m, memory: 256Mi}}
  memory-gateway:
    enabled: true
    appModule: viewsense_memory_gateway.app
    plane: memory-control
    tlsSecret: tls-memory-gateway
    clientId: memory-gateway
    clientSecretKey: memory-gateway
    ingressFrom: [orchestrator, smoke]
    egressTo: [identity, memory-postgres, memory-mem0]
    resources: {requests: {cpu: 50m, memory: 96Mi}, limits: {cpu: 500m, memory: 256Mi}}
  memory-postgres:
    enabled: true
    appModule: viewsense_memory_postgres.app
    plane: memory-provider
    tlsSecret: tls-memory-postgres
    ingressFrom: [memory-gateway]
    egressTo: [memory-db]
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}
  memory-mem0:
    enabled: true
    appModule: viewsense_memory_mem0.app
    plane: memory-provider
    tlsSecret: tls-memory-mem0
    ingressFrom: [memory-gateway]
    egressTo: []
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}
  mcp-gateway:
    enabled: true
    appModule: viewsense_mcp_gateway.app
    plane: mcp-control
    tlsSecret: tls-mcp-gateway
    clientId: mcp-gateway
    clientSecretKey: mcp-gateway
    ingressFrom: [orchestrator, smoke]
    egressTo: [identity, mock-mcp, registry-db]
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}
  mock-mcp:
    enabled: true
    appModule: viewsense_mock_mcp.app
    plane: mcp-provider
    tlsSecret: tls-mock-mcp
    ingressFrom: [mcp-gateway]
    egressTo: []
    resources: {requests: {cpu: 25m, memory: 64Mi}, limits: {cpu: 250m, memory: 192Mi}}
  agent-runtime:
    enabled: true
    appModule: viewsense_agent_runtime.app
    plane: agent-execution
    tlsSecret: tls-agent-runtime
    ingressFrom: [smoke]
    egressTo: [agent-db]
    resources: {requests: {cpu: 100m, memory: 128Mi}, limits: {cpu: "1", memory: 512Mi}}

datastores:
  memory:
    image: pgvector/pgvector:pg16-bookworm
    credentialsSecret: memory-db-credentials
    storage: 2Gi
  registry:
    image: postgres:16-bookworm
    credentialsSecret: registry-db-credentials
    storage: 1Gi
  governance:
    image: postgres:16-bookworm
    credentialsSecret: governance-db-credentials
    storage: 1Gi
  agent:
    image: postgres:16-bookworm
    credentialsSecret: agent-db-credentials
    storage: 1Gi

networkPolicy:
  enabled: true
  dnsNamespace: kube-system
  dnsPodLabels: {k8s-app: kube-dns}
